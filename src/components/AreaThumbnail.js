import { Link } from "gatsby"
import React from "react"

const AreaThumbnail = () => (
  <div className="py-8 px-4 bg-grey-light rounded">
  	<h2>Stöd & Finansiering</h2>
  	<p>Se vilka stöd du som företagare kan få för att utveckla din verksamhet</p>
    <Link to="/AreaSingle/">Läs mer</Link>
  </div>
)

export default AreaThumbnail
